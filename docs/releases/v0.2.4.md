# Agent Hub v0.2.4

v0.2.4 修复旧房间数据库升级失败、后台扫描 metadata 超限，以及桌面进程在输出管道失效时可能被日志告警终止的问题。更新不改变协议版本或数据库 schema 版本。

## 新增内容

- 桌面端新增本机 UTF-8 JSONL 诊断日志，默认写入 `%APPDATA%\agent-hub\logs\desktop.log`。单文件最多 2 MiB，保留 3 个轮转文件。
- 日志只记录时间、级别、来源和错误的名称、代码、消息、堆栈；令牌、请求头、环境变量和扫描正文不会写入日志。
- 扫描 metadata 新增依赖与规则哈希的原始总数、实际上传数和截断标记，便于确认大仓库快照是否经过容量收敛。

## 问题修复

- 修复 v0.2.3 从 schema 3 升级时先创建 `local_scans(finalization_id)` 索引、后补 `finalization_id` 字段，导致 SQLite 报错并回滚整个迁移、Agent Hub 无法启动的问题。迁移现在先幂等补齐字段，再创建索引；失败回滚或手工补列后的数据库都可安全重试。
- 修复后台扫描只按 500 条依赖固定截断、却未遵守服务端 32,000 字符 metadata 上限的问题。扫描端与服务端现在共享同一上限，并按最终 `JSON.stringify(metadata).length` 保留规则哈希和依赖的稳定前缀；固定 metadata 自身异常超限时会在本机受控失败，不发送非法请求。
- 修复 Electron 主进程继承的 `stdout`/`stderr` 管道失效后，`console.error` 触发 `EPIPE` 并终止桌面进程的问题。流保护在桌面生命周期启动前安装，服务子进程输出直接写入诊断日志，日志写入、轮转或关闭失败均静默降级。

## 兼容性与升级

- 应用版本：`0.2.4`
- 协议版本：1
- 数据库 schema：4
- 最低可读取来源 schema：2

数据库迁移覆盖 v0.1.0 的 schema 2，v0.2.0/v0.2.1/v0.2.2 的 schema 3，v0.2.3 失败回滚后的 schema 3，已经手工补齐 `finalization_id` 但 `user_version` 仍为 3 的恢复状态，以及已迁移的 schema 4 和全新数据库。

- v0.2.2：可直接使用应用内更新。
- v0.2.0/v0.2.1：下载 `AgentHub-Setup-0.2.4-x64.exe`，在原安装目录手动覆盖安装。
- 无法启动的 v0.2.3：必须手动覆盖安装 v0.2.4，因为应用内更新入口不可用。
- 已修复或已正常迁移到 schema 4 的 v0.2.3：可正常使用应用内更新。

所有情况都不要先卸载 Agent Hub，不要删除 `%APPDATA%\agent-hub`，也不要手工删除房间 SQLite 数据库。覆盖安装会保留房间、成员、连接、凭证、租约、扫描和功能记忆。

## 已验证

- 两套 TypeScript 配置检查、59 个测试文件共 456 项串行全量测试、生产构建和 `git diff --check` 通过；扫描、真实服务端接口、日志和 supervisor 的 4 个定向测试文件共 23 项通过。
- 数据库迁移测试覆盖上述历史状态；每种状态均保留房间、成员、会话、租约和扫描记录，最终达到 `user_version=4`，目标字段与索引存在，并通过 SQLite `integrity_check` 和 `foreign_key_check`。
- `D:\UGit\CombatDemo` 只读实测产生 7,068 条依赖和 1 条规则哈希；裁剪后保留 238 条依赖和 1 条规则哈希，最终 metadata 为 31,938 字符，并被真实扫描接口接受。
- 隔离 Electron 候选在主进程 `stdout`、`stderr` 各模拟一次 `EPIPE` 后继续运行；两条错误和本地服务启动记录均写入隔离诊断日志。
- Windows x64 unsigned NSIS 本地候选为 119,726,009 字节，SHA-256 `005E817C81E5C7E265A6C84A290A45F0DBE77EB20FA49A1FB349327481374EAD`，Authenticode 状态为 `NotSigned`。
- 本地候选 `app.asar` 为 46,720,471 字节，SHA-256 `59FCC9FF1DB72BAFC8A044B70291FDB66B27554B786C565A61323BE59C92E8E7`；物理文件恢复备份一致，备份健康、MCP bridge 和 Codex Hook 探针均返回 `ok`。

## 尚未完成的真实验收

- 未在当前 `D:\AgentHub` 安装上执行覆盖升级，也未读取或修改现有房间数据。
- 真实 v0.2.2 应用内更新到 v0.2.4、无法启动的 v0.2.3 手动覆盖恢复，以及非默认安装目录升级仍需在独立验收环境执行。
- 真实断网长时间恢复、双机协作和多房间切换不因本次自动化与隔离候选验证而视为已通过。

## 正式发布审计（2026-08-28）

- Annotated tag `v0.2.4` 指向提交 `5e32074d59af6bd205806219c84109817fba5bfa`；发布时与 `main`、`origin/main` 一致，发布后未移动标签。
- 主分支 CI `33140200506`、标签 CI `33140268556` 和 Windows Release `33140268602` 均成功。远端 publication gate 未配置，工作流按仓库已审计的 `AGENT_HUB_ALLOW_UNGATED_RELEASE=true` 显式继续并留下记录。
- GitHub Release `378252351` 于 `2026-08-28T04:05:32Z` 正式公开并设为 latest：[Agent Hub v0.2.4](https://github.com/lcx040126/agent-hub/releases/tag/v0.2.4)。
- 正式 Setup 为 119,725,884 字节，SHA-256 `8FA94558A62D485D00BE09432163290CD7978FE20D1B70872272F2BC666FF910`，Authenticode 状态为 `NotSigned`。
- 正式 blockmap SHA-256 `A2345A9239C688A8B20FA8ED9366759F3C9EE9402DF112E531A0C5FB623B905A`；`latest.yml` `8B703C3556A245132D0DA3307E021EE08B107BFFFC5A311662295B1D9A07B5BF`；更新清单 `CA6E9B172D20554DE5783D309EC4BE731698763007A9C1DD975B7CF2D965DBCE`；签名文件 `99A8B6FF291B9170D9C905864119F276DEE2D6E640EFF3B1CFB8C31242DEC4AA`；`SHA256SUMS.txt` `9815B82C20D89E42EFE924381F24A17DF83339987B95D743B3B9B25907F3FB40`。
- 六项资产的独立下载 SHA-256、GitHub asset digest 和校验文件一致；Ed25519 签名通过，更新清单的 v0.2.4、协议 1、schema 2 到 4、Setup URL/大小/SHA-256 和发布正文均正确。`latest.yml` 的大小与 Setup 一致，SHA-512 为 `q7v0nihAMNQTslu6UasfGGS8k1gvtYkZkgqFhA3Ldi88BBlgxYubfMGauPbZY0CGcKZKqPpxmCSSzmy6uKBdzg==`。
- 匿名访问 `releases/latest`、latest 更新清单、签名和正式 Setup 均返回 HTTP 200 并解析到 v0.2.4。以上审计段落在公开后追加，不修改标签中的发布说明或已签名清单正文。
