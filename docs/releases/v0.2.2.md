# Agent Hub v0.2.2 发布说明

> 本说明明确区分已经通过的自动化验证与仍待完成的真实安装、多机和后续热更新验收。未列为通过的项目不得视为已经验收。

> **v0.2.0/v0.2.1 升级要求：** 请从本 Release 手动下载 `AgentHub-Setup-0.2.2-x64.exe`，完全退出 Agent Hub 后覆盖原安装目录。不要在旧客户端内继续点击更新，不要先卸载，也不要删除 `%APPDATA%` 中的 Agent Hub 用户数据。

v0.2.2 聚焦三个直接影响本机日常使用的问题：同一个本地项目不能同时被多个房间接管；不再需要的已保存房间应能从本机完整移除；v0.2.0/v0.2.1 的更新恢复不能正确复制 `app.asar`。

## 本版变更

### 1. 每个规范化本地仓库只有一个活动房间

同一台电脑仍可保存多个房间，也可以让不同房间指向同一个本地项目，但 Hook、MCP、后台扫描、心跳和通知只能由其中一个活动房间驱动。

- 仓库身份使用解析后的规范化路径；Windows 路径大小写和可解析的路径别名不会制造多个 owner。
- 创建、加入或打开一个房间时，连接先以暂停状态保存，再通过仓库级串行切换启用，避免并发操作短暂产生两个 owner。
- 切换房间会先暂停同项目旧房间，清除旧 Hook 会话并尝试清理远端会话、租约和申请，然后再启用所选房间。
- 升级时如果旧 `connections.json` 为同一仓库留下多个活动连接，启动迁移会把这些候选全部暂停，等待成员明确选择，不按保存顺序猜测。
- 嵌套 Git 仓库按最深的匹配根目录路由，父仓库连接不会抢走子仓库任务。

旧房间断网、超时或返回 HTTP 408、429、5xx 时，本机暂停仍立即生效，未完成的远端清理显示警告。所选房间和 Codex 写入不会只因为暂时性网络故障被阻止。认证失败、服务器明确拒绝清理、本机凭证或本地清理损坏仍属于硬错误，不会被伪装成成功切换。

如果异常状态仍让同一仓库存在多个活动候选，Hook 将其作为软失败处理：`PreToolUse` 放行并明确说明本次没有完成跨成员协作检查，`SessionStart` 注入同类警告，后台扫描和通知跳过该仓库。系统不会随机连接某个房间，也不会把歧义误报成真实租约冲突。

### 2. 从本机移除已保存房间

登录页历史列表、当前房间“连接设置”和房间离线错误页都提供“从本机移除”。该操作与“退出房间”不同：

- “退出房间”只暂停连接，保留成员身份、加密凭证和历史入口，以后可以重新打开。
- “从本机移除”删除该连接记录、DPAPI 加密成员凭证、Hook 会话、暂停准备/重试状态和本地操作标记，并清除当前界面的会话指针。
- 在线时先尝试关闭本人的活动会话、释放租约并取消相关申请；房间暂时离线时本机删除仍可完成，但远端状态可能等待服务器恢复或租期到期。
- 该操作不会解散远端房间，不删除共享记录、功能记忆、其他成员数据或房主 SQLite 数据库。
- 删除房主自己的本机连接时，只有服务器刚确认当前成员仍是房主、远端清理已经完成且没有其他活动本机连接仍使用该服务，才停止本机房间服务。
- 本机连接已经删除后，如果房主服务未能立即停止，删除结果仍会成功返回并显示警告；应用退出时会再次尝试停止服务，不会留下无法重试的已删除房间入口。
- 所有本机删除操作（包括不同仓库的连接）通过同一个全局串行队列执行，避免并发读取和写回 `config.toml` 时相互覆盖。

### 3. 精确清理 Codex MCP 与共享 Hook

删除本机房间时，Agent Hub 会读取 `~/.codex/config.toml`，但不会按名称前缀宽泛删除：

- 只有 MCP 名称、当前 `Agent Hub.exe` 命令、完整参数列表和唯一的 `--connection-id <当前连接 ID>` 全部匹配时，才移除该房间的 `agent_hub_*` 条目。
- 其他房间、用户编辑过的同名配置、未知旧版本条目和非 Agent Hub MCP 均保留。
- 只有最后一个可识别的 Agent Hub MCP 被移除、且不存在其他潜在 `agent_hub_*` 条目时，才尝试移除共享 Hook。
- 删除最后一个 Agent Hub 连接时会正确清理仍保持精确受管定义的共享 Hook；只要还有其他连接或潜在 MCP，Hook 就继续保留。
- Hook 删除同样要求命令、前置参数、事件、matcher 和 timeout 与受管定义精确匹配；用户自定义或修改过的处理器保留。
- 实际改写配置前生成新的时间戳 `.bak` 备份并原子替换；配置发生变化时提示重启 Codex。

保守留下无法确认归属的配置比误删用户配置更安全。本机房间数据删除和 Codex 配置清理是两个边界：精确匹配失败不会扩大删除范围。

### 4. 修复 `app.asar` 更新恢复备份

v0.2.0 和 v0.2.1 的恢复准备通过 Electron 主进程中的 `node:fs/promises.cp` 递归复制整个安装目录。Electron 会改写普通 `node:fs`，把路径中的 `.asar` 当作虚拟归档处理。当复制目标到达恢复目录的 `resources/app.asar` 时，目标文件尚未形成有效归档，Electron 却尝试解析它，于是抛出：

```text
Error: Invalid package ...\application\resources\app.asar
```

源 `app.asar` 可以正常解析和计算哈希；使用 Electron 的 `original-fs` 复制同一文件后，源与备份字节一致。因此该错误不是 Codex 占用、安装到 D 盘、源归档损坏或更新安装器下载失败。

v0.2.2 的恢复准备改为：

- 使用 `original-fs` 对安装目录执行物理递归复制；
- 使用物理 `stat` 确认备份中的可执行文件和 `app.asar` 都是实际文件；
- 比较源与备份 `app.asar` 的文件大小和 SHA-256，只有字节一致才写入待恢复计划；
- 使用同一物理文件系统清理恢复备份，避免异常路径再次触发 ASAR 虚拟目录处理。

## v0.2.0/v0.2.1 必须手动覆盖升级

这项修复位于执行更新准备的客户端自身。v0.2.0/v0.2.1 会在启动新安装器和写入 `pending` 恢复计划之前失败，因此已经下载的 v0.2.2 文件无法反向修补仍在运行的旧更新器。

升级到 v0.2.2 时，房主和每位成员都必须完成一次外部 Setup 覆盖：

1. 暂停新的 Agent 写入并退出 Agent Hub。
2. 从正式 Release 下载 `AgentHub-Setup-0.2.2-x64.exe`，核对 `SHA256SUMS.txt`。
3. 运行 Setup 并选择原安装目录覆盖安装；不要先卸载，也不要删除 Agent Hub 用户数据。
4. 启动 v0.2.2，确认原房间、连接、加密凭证和 Codex 接入仍在。
5. 房主先确认原房间可打开，再让成员逐台覆盖并恢复工作。

退出 Codex、重复点击热更新或删除更新缓存不能修复旧客户端代码。完成这一次覆盖后，后续版本才使用 v0.2.2 的物理备份与恢复路径。

## 兼容性与数据边界

v0.2.2 保持 Agent Hub 协议版本 1 和 SQLite schema 版本 3，并继续读取 v0.2.0/v0.2.1 的房间数据库与连接文件。

- 同项目唯一活动房间只约束本机接入选择，不合并或删除远端房间。
- 覆盖安装不应主动删除房间数据库、项目连接、加密凭证、风险策略、协作记录或功能记忆。
- “从本机移除”是成员明确选择的数据删除操作，不属于覆盖安装迁移。
- 网络软失败允许本次写入继续，但表示实时协作检查没有完成，不等于跨成员兼容性已经验证。

## 验证状态

### 已建立的自动化证据

| 检查 | 当前证据 | 边界 |
| --- | --- | --- |
| Electron 真实 ASAR 回归 | 同一集成测试在旧实现上稳定复现 `Invalid package`，在修复后通过 | 使用真实 Electron、`ELECTRON_RUN_AS_NODE=1` 和有效 `default_app.asar`；确认备份是物理文件、大小相同、SHA-256 相同且 `pending` 计划成功落盘 |
| 仓库 owner 与路由 | 已加入规范化路径、暂停连接过滤、最深仓库匹配和多 owner 歧义测试 | 自动化代码路径不能替代真实多个 Codex 任务同时运行 |
| 独占切换与删除生命周期 | 已加入同项目切换、远端软失败、本机清理和删除结果测试 | 仍需在真实在线/离线房间和系统托盘生命周期中验收 |
| Codex 配置清理 | 已加入精确 MCP 身份、`--connection-id`、共享 Hook 保留/删除和用户配置保护测试 | 仍需用真实 Codex 重启后确认最终加载状态 |

发布前本地候选曾完成以下验证：

- `pnpm typecheck`、生产构建和 `git diff --check` 通过；全量自动化测试共 50 个文件、347/347 项通过。
- Windows x64 NSIS 候选 `AgentHub-Setup-0.2.2-x64.exe` 为 119,682,910 字节，SHA-256 `98278A2B204B11AFDCDA99D9442FBB3F01F837D3F8356954C5903ABB69F4F2AE`。
- 打包恢复探针验证候选中的 `app.asar` 为物理文件，大小 46,418,738 字节，SHA-256 `A718E5F4BD76BBDC7394B28C3743801D21D01B077134C80F090DFD60158A88B9`，并返回 v0.2.2、MCP bridge 与 Codex Hook 均为 `ok`。

### 尚未完成的真实验收

- 在保留真实 `%APPDATA%` 数据的前提下，分别从已安装 v0.2.0 和 v0.2.1 手动覆盖到 v0.2.2，并确认房间、成员、项目连接、DPAPI 凭证和 Codex 配置保留。
- 在两个房间指向同一项目的真实界面流程中切换活动房间，验证旧房间在线、断网、认证失败和恢复后的提示与远端清理。
- 从登录历史、当前房间和离线错误页分别删除连接，核对本机文件、`config.toml` 备份、精确 MCP/Hook 清理和 Codex 重启结果。
- 对 Windows x64 发布候选执行全新安装、原目录覆盖、非默认安装盘、托盘退出、房主服务保留/停止和卸载前清理验收。
- 使用真实未来版本验证 v0.2.2 之后的应用内下载、物理安装备份、安装重启、健康确认、失败回退和缓存清理。
- 在至少两台真实电脑上验证房主/成员、真实 Codex Hook/MCP、断网软失败和并行写入行为。

这些项目完成并记录之前，自动化测试通过不能视为 v0.2.2 已完成真实升级、桌面交互或多机验收。

## 正式发布核验

`v0.2.2` 已于 `2026-08-27T10:02:07Z`（北京时间 2026-08-27 18:02:07）正式公开：[Agent Hub v0.2.2](https://github.com/lcx040126/agent-hub/releases/tag/v0.2.2)。Annotated tag、`main` 与 `origin/main` 在发布时均落到提交 `951b38e33cc1a3f1b5421cf704aabf5a511c30a1`。

- 主分支 [CI 33059912420](https://github.com/lcx040126/agent-hub/actions/runs/33059912420)、标签 [CI 33060063878](https://github.com/lcx040126/agent-hub/actions/runs/33060063878) 和 [Windows Release 33060063757](https://github.com/lcx040126/agent-hub/actions/runs/33060063757) 均成功。
- Windows Release 的发布 gate 配置检查通过；远端 gate 因未配置而跳过，仓库显式启用的 ungated release override 已被工作流记录。
- 正式打包恢复探针返回 `status=ok`、`version=0.2.2`、`backupHealth=ok`；`app.asar` 为 46,418,738 字节，SHA-256 `A718E5F4BD76BBDC7394B28C3743801D21D01B077134C80F090DFD60158A88B9`。
- Setup 的 Authenticode 状态为预期的 `NotSigned`。项目尚未提供发布者代码签名，因此用户仍应通过本节哈希验证下载内容。

| 正式资产 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `AgentHub-Setup-0.2.2-x64.exe` | 119,682,857 | `3D815A1AF94A2344A39F3506B3884D6E3550A6ABFBC0EF18D8DAA7FF3D736802` |
| `AgentHub-Setup-0.2.2-x64.exe.blockmap` | 126,320 | `5B6D20725C1FA20A5A58DCDEB432844E8590F4B5F643BE7FE57B84AB60241019` |
| `latest.yml` | 353 | `5FC9D5E8FD6179E2D57B4CC63906B4AA086E6DD97CA288382CA122B66F9F382A` |
| `agent-hub-update.json` | 11,962 | `6763110B1003C567B5B287FC0B262B2946576183C4033F9CE7F53E6331031EFE` |
| `agent-hub-update.sig` | 89 | `F4D96AD9E491048A9146EA019DCE628B3EDEED27993B65F8AD9B0DF1B583F6D4` |
| `SHA256SUMS.txt` | 456 | `C8E086BC66D852A567C7118DFF0655349D038A3AD067FE469CE546FCD029E7D8` |

六项资产均为 uploaded，下载后的大小和 SHA-256 分别与 GitHub asset digest 一致；`SHA256SUMS.txt` 按设计列出的五项可自校验资产也全部匹配。`agent-hub-update.json` 的 Ed25519 签名验证通过，清单版本为 0.2.2，协议兼容范围为 1..1，schema 兼容范围为 2..3，可信下载 URL、Setup 大小、SHA-256 和发布时说明正文均匹配。

`latest.yml` 的版本、文件名、119,682,857 字节大小和 Setup SHA-512 `fLTVrJcYHj0lW7bvEXFxhteNcOq39LdCAc7cP2YGyNxjCW0Geys8GNHB6Q60uSyCggISJHSoKuN8SsBHTPGs0w==` 一致。匿名访问 `/releases/latest/download/agent-hub-update.json` 与对应签名均返回 HTTP 200，下载字节与已审计资产一致并可再次验签为 v0.2.2。

签名清单的 notes 与公开时的 Release 正文及标签提交中的本文件一致；本节是在正式公开后追加的审计记录。上文“尚未完成的真实验收”仍然有效，不因 Release 已公开而视为通过。
