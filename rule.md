# Agent Hub 产品原理与团队使用规则

> 本文说明 Agent Hub v0.2.7 当前源码候选的协作模型、使用规则和能力边界。当前正式安装版本仍为 v0.2.6，v0.2.7 尚未发布正式安装资产。本文不是 Codex 默认加载的项目指令文件；稳定、人工批准的项目规范仍应写在仓库的 `AGENTS.md`、`ProjectRules` 或团队约定的规则文件中。

## 1. Agent Hub 解决什么问题

单独一份 `AGENTS.md` 能规定编码习惯和架构原则，却不知道谁正在修改什么、刚完成了哪些行为、哪些测试失败、哪个租约即将到期，也不能在 Agent 写入前执行检查。

Agent Hub 为所有成员提供同一份实时、结构化、可审计的项目协作状态，并把读取和检查接入 Codex 工作流。它重点降低两类风险：

1. 两个 Agent 同时修改重叠文件或关键资源。
2. 后来的任务不了解已经完成的功能行为，意外破坏原有逻辑。

它不能把多个模型变成同一个“大脑”，也不能读取其他人的私人对话或隐藏思考。它记录的是能够被项目证据支持的协作事实。

## 2. 系统原理

```text
房主电脑上的权威房间服务
  ├─ SQLite：成员、会话、租约、策略、申请、功能修订、验证和审计
  ├─ REST：桌面客户端、本地扫描器和 Hook 使用
  ├─ MCP：Codex 按需查询上下文并执行协调操作
  └─ 局域网地址：成员通过邀请地址和房间码加入

每位成员的电脑
  ├─ Agent Hub 桌面端：连接、设置、待处理申请和 Windows 通知
  ├─ 本地扫描器：分析 Git、C#、Unity 和项目规则
  ├─ MCP bridge：按需获取共享状态和功能记忆
  └─ 六类 Codex Hook：会话开始、提示提交、写入前后、回合停止和会话结束
```

房主服务是当前房间状态的权威来源。房主电脑离线时，成员不能取得最新状态；离线不会自动批准释放请求，也不会擅自把他人的工作标记为完成。

Agent Hub 与 Git 的职责不同：Git 保存源码和提交历史，Agent Hub 保存协作现场和功能证据。发生矛盾时，以当前源码、权威配置、人工批准的规则和真实验证结果为准。

## 3. 房间中保存什么

房间保存或同步：

- 房间、项目、成员、Agent 会话和角色，以及 Hub Session、Codex Session、当前 Turn 和 activity epoch 等会话定位信息；
- 分支、Git 基线、实际变更路径和提交证据；
- 当前任务意图、租约类型、托管方式、创建来源、路径、有效期和冲突结果；
- 房主风险策略、策略版本和保护开关；
- 持久化释放申请及其处理结果；
- 功能名称、行为契约、符号、路径、依赖、测试、修订关系和当前有效版本；
- 决定、验证、风险、交接和协调操作的作者与时间，以及调用级范围覆盖、扩展和实际变化归因。

房间默认不上传：

- 模型私人聊天、隐藏思考或完整上下文窗口；
- 源码全文和补丁正文；
- 环境变量、密钥、Git 凭证、私人提示词和未主动登记的笔记。

本地扫描器可以读取项目文件来提取结构，但上传的是路径、哈希、符号、系统和依赖等结构化元数据。成员手工登记内容时仍不得粘贴敏感信息。完整 Hub Session、Codex Session 和 Turn 标识只通过成员鉴权接口显示，不是访问凭据；匿名导出、房间上下文导出和公开诊断不得包含系统保存的完整标识。

## 4. 安装、创建和加入

正式发布版本使用 Windows x64 NSIS Setup 安装版，安装包名为 `AgentHub-Setup-版本号-x64.exe`。房主和每位成员都必须安装自己的客户端，并使用同一个正式发布版本；当前正式版是 v0.2.6，本地 v0.2.7 源码或构建目录不得当作已经发布的安装资产。

房主创建房间时选择本机 Git 项目，随后把局域网邀请地址和房间码发给成员。成员选择自己电脑上对应的同一仓库后加入；本地绝对路径不要求一致。中途加入者会读取房间已经保存的结构化状态，但无法读取未登记的历史聊天。

房间码不等于 GitHub 仓库权限。能加入 Agent Hub 房间不代表能向 GitHub push；仓库协作者、分支保护和 Pull Request 权限必须另行配置。

当前房主模式使用局域网 HTTP，只允许在可信专用网络中使用。不要把桌面房间通过路由器端口映射直接暴露到互联网。

## 5. Codex MCP 与 Hook

加入房间后，每位成员在“连接设置”中执行一次“安装连接”，然后重启 Codex。Agent Hub 会备份现有 `~/.codex/config.toml`，添加一个房间专用 MCP 和六类 Hook。非托管 Hook 首次执行前必须由用户核对并信任，Agent Hub 不会绕过 Codex 的安全审核。

六类 Hook 的职责：

- `SessionStart`：根据 Codex 会话的工作目录匹配本地项目，打开独立房间会话，注入预算内的实时状态和功能索引。
- `UserPromptSubmit`：在每次新提示开始时登记真实 turn 并推进 activity epoch，避免会话启动时的回退 turn 延续到首次写入。
- `PreToolUse`：为每次调用生成 `invocationId`，通过 PowerShell 官方 AST 和具体命令的路径参数识别目标，领取或扩展任务范围，再判断实时租约冲突和历史功能影响。
- `PostToolUse`：复用同一 `invocationId` 对比调用前后的工作区，以真实 Git 变化补充实际路径，只同步能够归因到本次 Agent 写入的变化。
- `Stop`：在宿主 3 秒预算内只持久化幂等 completion job 和活动围栏并立即返回；后台 worker 负责服务端 Stop、Git 证据判断、重试和最终释放。
- `SessionEnd`：把最终 Git 与验证证据排入持久队列，创建候选功能修订并结束 Hub 会话；证据不足的 Agent 托管租约独立保留到后续证明完成或 TTL 到期。

MCP 用于按需查询索引、功能详情、版本历史和协调状态，也用于登记只对当前会话有效的功能修改确认。MCP `lease_acquire` 固定以 `agent/mcp` 领取，Hook prepare 固定以 `agent/hook` 领取；同一 Hub Session 的两条入口取得同一份 canonical Agent 租约。Hook 负责自动触发时机，两者缺一都会降低保护完整性。

每个 Codex 会话都有独立的 Agent Hub 会话 ID。Hub Session 是 Agent 租约的唯一合并键；不同 Hub Session 不按任务标题或 Codex Session ID 合并。同一成员已有的人工写范围可以覆盖 Agent 请求路径，但只记录本次调用归因，不转换托管关系、不扩展 Agent 租约，也不由 Agent 生命周期释放。Hook 只在工作目录位于已连接仓库时匹配；项目外任务不会因为后来在命令中写了某个路径就自动改绑房间。

### 5.1 本机接入隔离与生命周期

本机连接保存 `integrationEnabled` 状态。v0.2.0 或更早的连接文件没有该字段时按 `true` 读取，以保持兼容；连接的暂停、恢复和令牌保存必须串行写入，不能让并发操作覆盖接入状态或成员凭证。

桌面进程维护一个包含实例 ID、PID、运行状态和心跳时间的本地哨兵文件，默认约每 5 秒原子刷新。Hook 和 MCP 在访问房间前都必须同时确认连接已启用且哨兵有效。文件缺失、损坏、PID 不存活、状态为 `stopped` 或 `maintenance`，或心跳超过约 30 秒未更新时，接入视为 inactive。

inactive 时的规则：

- Hook 完全静默，不连接服务端、不创建或恢复房间会话、不注入上下文、不登记活动、不发出冲突提示，也不阻止 Codex 写入。
- MCP bridge 仍可完成本地初始化握手，但远端连接采用懒加载；停用或暂时无法连接房间时，工具列表为空，不访问房间服务。
- 恢复有效连接和运行哨兵后，Hook 可建立新会话，MCP 在下一次工具请求时自动重连并刷新工具列表。

网络超时、断网、HTTP 408、429 和 5xx 等暂时性故障按软失败处理：写入继续，并向 Codex 提供黄色诊断，不能把房间暂时离线误判为代码冲突。保护模式继续按既有规则处理真实租约冲突、冻结会话、认证失败、无效或损坏的本机凭证；纯监测模式下这些异常只报告并放行，只有服务端在线明确确认的其他成员手动独占仍可拒绝写入。

退出与更新必须使用不同的生命周期语义：

- **退出房间**：只将所选连接持久化为暂停，先使其本机 Hook/MCP 失效，再请求房间清理本人状态。重新启动 Agent Hub 后该连接仍保持暂停，直到成员主动重新启用。
- **关闭主窗口**：只隐藏到系统托盘，后台、哨兵、Hook/MCP、会话和租约继续运行。
- **托盘“退出”**：先停用全部本机接入。没有未到期完成任务的连接在停止后台和房间服务前尝试完成本人远端清理；存在完成任务的连接不发送成员暂停，保留本地队列、会话及该连接的全部远端租约，供桌面重启后继续检查，或由各自 TTL 到期。此例外不适用于显式退出房间、暂停或删除连接。
- **房主退出**：先清理房主本人的会话和租约，再停止本机房间服务。数据库和房间历史保留，其他成员仅暂时离线。
- **安装更新**：进入 `maintenance`，使 Hook/MCP 暂停，但不关闭远端会话、不释放租约。更新失败并恢复旧版本后，原先未主动退出的连接恢复启用。
- **异常崩溃**：哨兵在最多约 30 秒后自然失效，使本机 Hook/MCP 停止影响 Codex；不能承诺崩溃会远端释放手动独占租约，普通租约仍按服务端到期规则处理。

主动暂停通过 `POST /api/member/pause` 发送 `reason`、`cutoffAt` 和 `requestId`。`cutoffAt` 是客户端请求身份参数；实际清理边界必须由房间服务器首次处理请求时使用服务器时钟确定，不能用成员电脑的时钟与服务器生成的会话时间直接比较。服务端鉴权后必须幂等地在同一事务内关闭当时可见的本人会话，释放本人全部活动租约，取消相关释放申请和过期的功能修改确认，同时保留成员身份、令牌、房间历史、数据库和功能记忆。同一 `requestId` 重试不得重复产生副作用；参数不一致时不得当作同一请求接受。

网络异常时，客户端先关闭本机接入，再把清理请求写入本地重试队列。重试必须沿用原 `cutoffAt` 和 `requestId`；存在待清理意图或请求时，Hook、MCP、后台扫描、Codex 心跳和桌面写请求不得创建新的远端状态。清理成功并移除队列记录后才可自动恢复远端能力，因此服务器首次处理时不会把恢复后的新会话或租约包含在旧请求中。

v0.2.7 使用协议版本 2、数据库 schema 6、最低来源协议 1 和最低来源 schema 2，并继续使用 Codex 配置中已有的稳定 Hook launcher 路径。升级对账只能校正能够证明属于 Agent Hub 且已经获得安装授权的 MCP 与六类受管 Hook；用户 Hook、其他 MCP 和无法确认归属的相似配置不得修改。

schema 6 把旧 `automatic` 标记为 `agent/legacy`，把 `standard/exclusive` 保守标记为 `manual/legacy`。只有 MCP 来源会话和对应 `lease.acquired` 审计共同证明来源时，旧 `standard` 才迁移为 `automatic/agent/mcp`。同一房间、成员和 Hub Session 的重复活动 Agent 租约以最早创建者为 canonical，合并路径、最长 TTL 和最保守状态，其余租约标记已合并并取消，历史冲突与活动不删除；迁移完成后由部分唯一索引保证每组最多一份活动 Agent 租约。

房主切换到纯监测模式前，所有现有成员都必须上报协议 2。界面根据成员上报的协议提前列出需要升级或尚未上报协议的成员；是否允许切换始终由房间服务再次校验。纯监测房间拒绝不满足协议 2 的新成员加入，避免旧客户端把红色 severity 错当作必然阻断。

同一 Hub Session 内的 MCP 与 Hook 调用合并到 canonical Agent 租约；同一成员的不同 Hub Session 发生重叠 Agent 写入时，仍以已经成功登记的活动租约为当前持有者，后来的会话等待现有持有者释放或到期，不得反向创建会阻塞当前持有者的对等冲突。系统不会仅凭创建时间、且在缺少该会话 Git 证据时强制取消其他任务。`UserPromptSubmit` 与 `PreToolUse` 的一次权威恢复共同处理真实 Turn/activity epoch 晚于 `SessionStart` 回退值的问题。

保护模式下，等待会话必须在本机持久化被动写围栏，并同时记录最近持有者与触发等待的完整申请范围。缓存 `expiresAt` 只能提示重新检查时机，不得由客户端据此认定持有者没有续租，也不得仅凭本机时钟清除围栏。只有明确路径覆盖完整原申请范围且房间服务返回允许后才能清除等待围栏；本地 `blocked` 标记也必须等明确路径检查成功后清理。即使会话 clean、自身租约已取消、本地租约状态曾丢失，或等待同步响应失败，也不得执行无法预先确定输出路径的生成、格式化和构建写入。复用远端会话但租约归属不完整时，路径未知写入必须拒绝，进入等待时必须按 dirty 处理，避免取消无法在本机归因的远端租约；这类状态与已知等待或阻塞围栏在明确路径网络检查失败时都不得降级为普通离线放行。纯监测模式会解除旧非独占等待或 blocked 标记，并把路径未知、状态损坏、同步失败和回合异常统一降为告警；跨成员手动独占仍按在线权威结果拒绝。

房主身份只决定管理权限，不绕过桌面代理的响应容量限制。仪表盘轮询不得携带累积扫描详情；只返回当前成员最近一次扫描摘要，并将会话和活动收窄为当前界面所需的数据。风险规则属于可整表保存的核心设置，必须完整返回，不能参与预算裁剪；待处理申请必须先按当前持有人查询再应用展示上限。其他可增长列表按 768 KiB UTF-8 响应预算装入，并返回数据库真实总数；SQL 展示窗口或字节预算任一处截断都以结构化状态标记为暂未完整刷新，不得把这类非网络错误误报为房间离线。完整证据仍保存在数据库和专用查询中。

### 5.2 会话定位与范围历史

“实时工作”任务卡按 `lease.sessionId` 绑定 Hub Session；已绑定的会话不再重复显示成独立行。卡片显示 Codex Session ID、Hub Session ID 和当前 Turn ID，默认采用“前 8 位…后 4 位”缩略并支持复制完整值；`activityEpoch` 和调用历史放在详情中。人工任务显示“未关联”，Hub Session 已存在但旧客户端没有提供 Codex/Turn 标识时显示“客户端未提供”，不得用另一类 ID 填充空值。

新 Turn 只更新任务卡当前 Turn，不创建重复任务。每次 Pre/Post 调用仍把当时的 Turn ID、来源、工具、阶段及路径归因保存为历史事件；详情展开时通过成员鉴权的分页接口懒加载，当前值更新不能覆盖历史。

## 6. 实时租约与风险策略

### 6.1 租约类型、托管方式与来源

- `automatic`：Agent 托管任务范围，由 MCP、Hook 或兼容迁移创建；持续工作时通过心跳续期。
- `standard`：成员主动声明的人工普通工作范围，按房间风险策略判断重叠。
- `exclusive`：成员主动创建的人工独占保护，采用固定到期时间，不被心跳延长。

`kind` 描述租约行为，`managedBy=manual|agent` 描述生命周期所有者，`createdVia=ui|mcp|hook|legacy` 描述可信入口。Agent 托管租约必须是 `automatic`，人工租约必须是 `standard` 或 `exclusive`；来源由服务端入口固定，公共请求不能自行伪造。

同一房间、成员和 Hub Session 最多存在一份活动 Agent 租约。每次 Agent 领取都在一个事务中完成人工覆盖分流、跨成员冲突检查、canonical 租约创建或取得、路径并集归约、`read -> write` 升级、TTL 续期和审计写入。人工写范围已经覆盖的路径只返回覆盖归因；未覆盖路径才加入 Agent 租约。领取响应用 `coverage[]` 区分人工已覆盖、Agent 已覆盖和本次新增路径，同时保留原有 `lease` 字段供旧调用方使用。

目录与其子路径视为重叠，Windows 路径不区分大小写。系统在领取宽泛范围时和每次写入具体文件时都会判断，防止目录级结果掩盖内部关键文件。

### 6.2 黄色与红色

- 黄色 `warning` severity：普通警告，保存重叠成员、路径、规则和原因。
- 红色 `blocking` severity：高风险警告，强调重点范围，但不单独决定能否继续。
- `decision=warn`：允许对应写入或领取继续；红色 severity 与 `warn` 可以同时出现。
- `decision=deny`：阻止对应写入或领取，并按冲突类型进入释放申请或确认流程。客户端不得根据 severity 自行补成 `deny`。

黄色不代表内容一定兼容，红色也不代表另一方一定正确。它们只描述风险等级；执行决定以房间服务返回的 decision 为准。

### 6.3 默认文件策略

| 分类 | 默认级别 |
| --- | --- |
| 普通源码 | 黄色普通警告 |
| Unity 场景与 Prefab | 红色高风险 |
| Unity 序列化资源与 `.meta` | 红色高风险 |
| 项目设置和包清单 | 红色高风险 |
| Git/CI 配置 | 红色高风险 |
| 结构化配置与普通表格 | 红色高风险 |
| Luban 源表、生成文件和相关目录 | 黄色普通警告 |
| 整个仓库范围 | 红色高风险 |
| 其他文件 | 黄色普通警告 |

房主可以按预设分类、扩展名、具体项目相对文件或项目相对目录自定义。匹配优先级依次为具体文件、最长匹配目录、扩展名、预设分类。规则保存时创建新版本并记录操作者、时间和变更。

只有房主可以编辑策略或切换“写入阻塞保护”。开关关闭后进入纯监测模式：Agent 托管和人工普通租约仍按策略显示黄色普通警告或红色高风险警告，但 decision 统一为 `warn`；历史功能修改确认也降为提示。现有租约不被删除。跨成员既有人工独占仍返回 `deny` 并要求持有人批准；新建人工独占范围与任一活动租约重叠时仍可拒绝领取。同一成员的人工写范围命中 Agent 请求时只记录覆盖归因，不转换、不扩展，也不由 Agent 生命周期释放。

## 7. 历史功能记忆

### 7.1 生成与证据

编码任务结束时，Agent Hub 使用任务目标、结构化总结、实际写入路径、代码符号、依赖分析、Git 基线与提交、验证结果和剩余风险交叉生成候选功能修订，不能只相信自然语言总结。

一项功能记忆包括：

- 已实现的用户行为和不应意外改变的行为契约；
- 所属系统、相关文件、类、方法、公开接口、Unity 资源和配置；
- 依赖系统、关联测试、验证命令和运行证据；
- 开发成员、Agent 会话、分支、基线、提交状态、风险和修订原因。

没有可靠 Git 证据、没有必要验证或验证失败时，修订必须保持草稿、候选、冲突或待验证状态，不能声称已经稳定。

### 7.2 不可变修订和当前有效视图

功能以“系统 + 具体功能 + 行为契约”为单位维护，不按“最后修改的人 + 整个系统”覆盖。

- 每次变化创建不可变修订，并标明新增、扩展、替代、废弃、回滚或冲突。
- 新修订未明确改变的既有行为和兼容要求继续继承。
- 普通任务只读取每项功能当前有效版本；旧版本仍可沿版本链审计。
- 不同分支对同一行为产生互斥变化时，保留双方候选，不按提交时间或成员身份自动选胜者。
- 回滚会创建新的回滚修订并重新激活被恢复行为，不删除被回滚版本。

### 7.3 分层检索

功能记忆不会全量塞入每个 Codex 会话：

1. 会话启动层只注入实时租约、当前规则、高风险和相关功能索引，目标预算为 2,000 至 3,000 token。
2. 规划层按任务目标、路径、系统、符号、接口、测试和依赖返回相关性最高的 5 至 8 张简要记忆卡。
3. 确认相关后，每次只懒加载 1 至 3 项必要详情；低置信候选先返回标题和命中原因。
4. 完整 Git 差异、旧版本和测试日志只在确认修改、回归验证或审计时读取。
5. 同一会话缓存已经加载的功能版本，版本未变化时不重复注入正文。

即使某项记忆没有提前进入上下文，服务端写入前影响检查仍使用完整功能索引，不能因为上下文裁剪而绕过保护。

### 7.4 修改确认

修改同一文件中的无关代码通常不触发确认。触及已记录的类、方法、公开接口、行为契约、关联测试，或系统不能排除对 Unity 资源的影响时，Hook 会把功能名称、既有行为、符号、路径和判断原因交给 Codex。写入阻塞保护开启时先拒绝本次写入；纯监测模式下改为提示并允许继续。

写入阻塞保护开启时，Codex 必须在当前对话中询问正在执行任务的成员。无需等待原开发者或房主，但必须得到明确同意。授权只绑定当前会话、功能版本、目标符号和路径；会话结束、功能版本变化或范围变化后失效。拒绝或没有明确回答时，Agent 应改用新增接口、扩展点或其他兼容方案。纯监测模式下仍应说明影响，但历史确认只作建议，不构成写入门槛。

确认并完成修改后，需要重新验证关联行为并创建新修订。确认不是测试通过的证明，也不是永久绕过许可。

## 8. deny 冲突后的释放申请

实时租约冲突明确返回 `decision=deny` 后，Agent Hub 自动创建持久化、去重的释放申请。`decision=warn` 不创建申请，即使其 severity 为红色高风险。

申请包含请求成员、任务、冲突租约、重叠路径、原因、申请时间和剩余租期。租约持有人在 Agent Hub 运行或位于托盘时收到 Windows 通知，应用内待处理窗口始终以房主数据库状态为准。

Agent 托管或人工 `standard` 租约的持有人可以：

- 交出冲突范围；
- 暂不释放并说明原因；
- 查看完整详情。

批准时，服务端在一个事务中完成原租约冲突路径的释放和申请人领取，避免中间被第三方抢占。一个租约有多个独立路径时，未冲突路径继续保留。成员离线或应用关闭不会自动同意，申请在重新连接后仍可处理。

Agent 租约默认无活动到期时间为 10 分钟。房主可选择 5、10、15、30 或 60 分钟；只有会话仍 active、租约仍为 `working` 且不存在 completion job 时才由心跳续期。`awaiting_commit` 不再续期，未提交、部分提交或证据不足时保留到原 TTL。桌面端只为最近 5 分钟内发生过真实 Hook 活动的会话保活；已进入单次写入的构建、测试或 Unity 工具调用最长保活 2 小时。人工 `standard` 继续沿用原有心跳与 TTL 规则，人工 `exclusive` 固定到期。修改设置不会突然截短已经领取的有效租约。

## 9. 手动独占租约

任何房间成员都可以对一个或多个项目相对文件或目录创建独占租约。租期最短 5 分钟，最长由房主设置，默认上限为 24 小时。

独占租约规则：

- 固定时间到期，不被 Agent 心跳自动延长；需要继续保护时由租客主动续租。
- 租客本人的 Agent 请求命中该人工写范围时只记录覆盖归因，不创建重复 Agent 路径。
- 其他成员的 Agent 一律返回 `deny` 并要求批准，包括 Luban 文件；这是纯监测模式下仍保留的写入阻断例外。
- 租客可以批准、拒绝或查看释放申请。
- 一旦批准，整份独占租约结束，不只释放申请中的子路径；申请人只取得实际请求范围。
- 租客离线时，独占租约保留到固定到期时间，申请不会自动获批。
- Stop、自动完成和分支切换都不释放人工 `standard/exclusive`；显式暂停、移除成员或租约持有人批准交接仍按对应管理动作处理。

独占租约只约束接入 Agent Hub Hook 的 Agent，不能阻止人工使用键盘、IDE、Unity 或其他程序修改文件。它不是操作系统文件锁。

## 10. Agent 写入归因

Agent Hub 只对能够可靠归因到当前 Agent 工具调用的写入执行租约和风险检查。

写入前记录本次工具调用的工作区基线、明确写入意图、标准化目标路径和建议符号；写入后只对比本次调用造成的增量。Pre/Post 共享 `invocationId`，审计分别保存候选、已覆盖、新增、忽略和实际变化路径，以及 source、tool、stage 和当时的 Turn ID。由当前 Agent 明确启动且能识别的生成器、格式化器或构建副作用，可以关联到同一次调用。

以下变化不创建租约、不生成房间风险、不显示黄色/红色警告，也不隔离 Agent 会话：

- 成员通过编辑器手工修改；
- Unity、IDE 或其他外部程序自动保存；
- 无法可靠关联到当前 Agent 目标或已知副作用的变化。
- 仓库外系统临时路径，以及同次任务已经清理且没有留下真实 Git 变化的仓库内临时日志。

这些外部变化只在本机保留临时诊断，仍然属于用户已有工作。Agent 不得覆盖、删除或回滚。人工与 Agent 同时修改同一目标文件时无法按行可靠区分，因此该文件仍按 Agent 目标检查，并要求保留已有修改。

PowerShell 路径识别使用 `System.Management.Automation.Language.Parser`，只静态求值字符串字面量、同一脚本内可证明的常量变量，以及所有参数都可证明的 `Join-Path`，再按具体命令的命名或位置路径参数解析。变量名、`SilentlyContinue`、命令开关、枚举值和其他表达式片段不能被误当作路径；`Rename-Item` 的新名称相对源文件父目录解析。

Agent 自己发出的写命令如果无法证明仓库内真实目标，不能用“外部变化”分类逃避检查。目标未被已有范围覆盖时，保护模式必须在执行前拒绝，纯监测模式只告警并允许执行；`PostToolUse` 再以真实 Git 变化补充归因。

## 11. 自动更新、回退与数据保留

从 v0.2.0 开始，已安装客户端在启动后和运行期间检查固定 GitHub Release。发现新版后由本机用户确认，随后下载、校验、安装并重启。房主不能替其他成员自动安装，也不会在用户未确认时强制重启。

更新器校验：

- 固定的 `https://github.com/lcx040126/agent-hub` Release 来源；
- Ed25519 签名更新清单；
- 协议和数据库结构兼容范围；
- 安装包文件名、大小与 SHA-256；
- 稳定版本号，并拒绝降级和同版本清单替换。

应用内更新前会缓存已验证安装包并一致性备份 SQLite 房间数据库。本机连接配置和 DPAPI 凭证位于安装目录之外，安装器不应覆盖，但当前版本不会为它们额外制作独立备份。恢复守护成功布防后，新版本成功启动并报告健康才提交更新状态；健康检查失败时使用上一份可恢复安装包和数据库备份回退。确认新版本健康后，才清理更旧的安装缓存和临时备份，保留下一次更新需要的恢复基线。

更新安装期间使用 `maintenance` 状态暂停本机 Hook 和 MCP，但不执行退出房间清理，不释放现有会话和租约。安装或健康检查失败并回退后，原先未主动退出的连接应恢复；已经由成员主动退出的连接仍保持暂停。

更新应保留：

- 房间数据库、成员、项目连接和角色；
- 本机 DPAPI 加密凭证和 Codex 连接信息；
- 房间设置、风险策略和审计记录；
- 租约、待处理申请、功能记忆和完整修订历史。

### 旧版本升级路径

v0.1.0 没有完整客户端更新器，必须手动运行当前正式 v0.2.6 Setup 覆盖原安装目录。v0.2.0/v0.2.1 的旧更新器存在 `app.asar` 恢复备份缺陷，也必须手动运行 v0.2.6 Setup 覆盖安装。

无法启动的 v0.2.3 没有可用的应用内更新入口，必须手动覆盖安装 v0.2.6；已恢复或已正常迁移到 schema 4 的 v0.2.3、v0.2.2、v0.2.4 和 v0.2.5 可以正常使用应用内更新到正式版。

正式 v0.2.6 的 schema 5 迁移覆盖 schema 2、3、4。v0.2.7 源码候选的 schema 6 迁移自动探针已覆盖 schema 2、3、4、5、新建 schema 6 及中断重试，但正式安装覆盖升级、真实 Codex 多轮和双机协作尚未验收，因此不能据此建议安装 v0.2.7。正式安装包、更新清单、签名和校验和只以 GitHub Release 的六项资产为准，不得把本地构建目录描述成正式安装包。

升级前暂停新的 Agent 写入；先由房主升级并确认原房间可打开，再让成员逐台升级。所有路径都禁止先卸载或删除 `%APPDATA%\agent-hub`，也不要手工删除房间 SQLite 文件。安装覆盖、数据库迁移、失败回退、房主/成员重连和 Hook/MCP 兼容的真实验证状态记录在各版本发布说明中；未列为通过的项目不得仅凭设计说明视为已经验收。

自动更新需要安装并重启，不是无重启热替换。程序即使安装在 D 盘，Electron 用户数据、房间数据库和恢复缓存通常仍位于当前 Windows 用户目录中。当前安装包没有商业 Authenticode；Ed25519 只验证更新清单。若安装过程中系统重启或新程序完全无法启动，当前没有独立 RunOnce 恢复入口，可能需要人工重新安装或还原数据。

## 12. 对 Git 和发布流程的影响

Agent Hub 不安装本地 commit 或 pre-push Hook，不改写提交，不自动 merge、rebase、rollback 或 push。成员继续使用原有 Git 和 Pull Request 流程。

可选的 `gate:publish` 可以在 CI/Release 中检查房间已经登记的 `decision=deny` 冲突、未解决 critical 风险和相关范围的最新失败验证。房间明确处于纯监测模式时，这些发现仍完整输出，但命令退出码为 0；写入阻塞保护开启时继续执行拒绝。快照不可用时，只有仪表盘明确关闭阻塞保护才允许仅报告；仪表盘显示保护开启或无法确认设置时都返回退出码 2 并保持默认拒绝。正式 Release 未配置房间地址和凭证时，仓库必须显式设置 `AGENT_HUB_ALLOW_UNGATED_RELEASE=true` 才能发布，避免因漏配而静默绕过。门禁不能替代编译、测试、Unity Play Mode、代码审查或人工验收。

提交 Git 不等于 Agent Hub 会话必然结束。Agent 租约在 Stop 后进入 `awaiting_commit`；后台 worker 只有在归因改动已完整进入当前分支 HEAD 或已完全撤销时才自动释放，未提交、部分提交或证据不足时保留到原 TTL。SessionEnd 关闭 Hub Session 本身不释放仍待证据的 Agent 租约，人工 `standard/exclusive` 不参与这条自动链。

## 13. 事实优先级与能力边界

发生信息冲突时，按以下顺序判断：

1. 当前 Git 状态、源码、Luban 源表和项目权威数据；
2. 人工批准的 `AGENTS.md`、`ProjectRules` 和架构决定；
3. 当前活跃租约、已确认的功能修改范围和冲突处理；
4. 与具体提交绑定的真实验证结果；
5. Agent Hub 的结构化观察、风险和历史聊天摘要。

Agent Hub 不能保证零回归，也不能完整理解反射、动态调用、网络协议、运行时时序或所有业务数据语义。路径不重叠的代码仍可能通过接口和状态产生耦合。因此所有功能仍需执行项目规定的编译、测试、运行时验证和代码审查。

Windows 通知只是提醒入口；最终租约和申请状态以房主服务为准。房主离线、Hook 未信任、客户端未加入房间、连接已暂停或其他 Agent 未接入时，自动保护不完整。网络软失败期间允许继续写入意味着实时冲突检查暂时不可用，黄色诊断不能替代恢复连接后的检查、测试和代码审查。

## 14. 最简团队规则

1. 所有成员安装同一版本 Setup 客户端，选择自己的本地仓库并加入同一房间。
2. 每位成员安装 MCP/Hook 连接、重启 Codex，并审核信任六类 Hook。
3. 房主开发期间保持房间服务在线；普通开发无需手工登记租约或功能记忆。关闭主窗口只是隐藏到托盘，真正停止后台应使用托盘“退出”。
4. 黄色表示普通警告，红色表示高风险警告；两者都必须阅读重叠路径和原因。只有 `decision=deny` 不得绕过，应等待申请处理或改变实现范围。
5. 修改已有功能时，Codex 必须先说明影响；写入阻塞保护开启时取得当前成员明确确认，纯监测模式下把确认作为建议。无论模式如何仍要执行回归验证。
6. 需要明确保护文件或目录时使用固定到期的手动独占租约，不把它当作人工文件锁。
7. 不把房间码、成员凭证、密钥、源码全文或私人聊天写入协作记录。
8. 成员退出房间后，该连接保持暂停；需要再次使用时必须在 Agent Hub 中明确重新启用。
9. Git、Pull Request、分支保护、测试和代码审查继续作为最终质量保障。
