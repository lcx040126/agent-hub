# Agent Hub 产品说明与使用规则

> 本文件是 Agent Hub 的产品说明和团队使用说明，不是 Codex 默认自动读取的项目指令入口。需要每个 Agent 持久遵守的项目规范应继续放在仓库的 `AGENTS.md` 中；也可以由团队显式配置其他项目文档回退文件名。Agent Hub 会共享规则文件的路径和摘要，但不会用本文件替代项目规则。

## 1. 产品要解决的问题

多人使用不同 Agent 开发同一个项目时，每个 Agent 只知道当前成员提供给它的上下文。即使仓库中有统一的 `AGENTS.md` 或 `Agent rules.md`，静态规则也不知道下面这些实时情况：

- 谁正在修改哪个系统或文件；
- 对某个接口、状态机或资源格式已经作出了什么决定；
- 哪些验证刚刚失败，哪些风险尚未解决；
- A 的改动会通过哪些代码、Unity 资源或配置依赖影响 B；
- 中途加入的成员应该从哪里接上当前工作。

结果是：A 按自己的系统逻辑实现功能，B 在另一个 Agent 中开发耦合功能时看不到这套逻辑，可能改坏 A 已完成的行为。Agent Hub 的目标不是让所有模型真的拥有同一个“大脑”，而是给所有参与者一个共同、实时、可审计的项目协作状态，并把读取状态和写入前检查接入 Agent 的工作流程。

## 2. 核心原理

Agent Hub 使用“中心房间 + 本地组件 + Agent 接入 + 可选发布门禁”的结构：

```text
房主电脑上的房间服务
  ├─ SQLite：成员、工作范围、决定、验证、风险、交接、扫描和活动记录
  ├─ REST/MCP：向每位成员的本地组件和 Agent 提供同一份状态
  └─ 局域网地址：让其他成员加入

每位成员的电脑
  ├─ 本地扫描器：分析自己的 Git 项目，只上传结构化元数据
  ├─ Codex MCP：按需查询房间上下文和执行协调操作
  └─ Codex Hooks：在会话开始、写入前、写入后和会话结束时自动保护

GitHub / CI（可选）
  └─ 共享分支门禁：在合并或发布前检查未解决阻塞项
```

所有成员通过各自凭证访问同一个房间，因此中途加入者可以读取当前快照，而不需要依赖之前成员的聊天记录。

## 3. 共享的不是模型记忆

“共用记忆”在本产品中指共享结构化、可追踪的项目事实，不是复制模型的私聊或内部状态。

房间会保存或同步：

- 房间、项目、成员和会话身份；
- 分支、基准提交、工作树和仓库相对路径；
- 当前工作的意图、读写范围、租约有效期和冲突结果；
- 已确认决定、验证结果、剩余风险和交接记录；
- Git 变化路径、规则文件哈希、系统划分和依赖元数据；
- 谁在什么时候执行了什么协调操作的活动记录。

房间默认不上传：

- 模型私人对话、隐藏思考或完整上下文窗口；
- 源码全文和补丁正文；
- 环境变量、密钥、Git 凭证或其他本机秘密；
- 未经成员主动登记的私人笔记。

本地扫描器会在成员电脑上读取必要的项目文件来分析结构，但上传的是路径、哈希、系统和依赖等元数据。手工填写决定或风险时，成员仍有责任不粘贴敏感内容。

## 4. 七个协作机制

### 4.1 房间

房主创建房间后，房主电脑成为当前验证版的权威服务。其他成员使用邀请地址和房间码加入，每名成员会获得独立凭证。成员凭证在 Windows 本机由 DPAPI 加密保存，服务端只保存用于校验的 Token 哈希。

房主电脑退出、关机或离开网络时，成员会暂时无法同步。房主重新启动 Agent Hub 后，SQLite 中的房间记录会恢复。

### 4.2 实时快照

Codex 会话开始时，Hook 会根据当前项目目录找到已保存的房间连接，读取成员、活跃范围、决定、未解决风险、最近验证和本地分析结果，并把摘要加入本次 Agent 上下文。

所以后来加入房间的成员能够知道团队正在做什么。但它读取的是已经进入房间的结构化记录，而不是追溯其他人的私聊；没有登记、没有扫描到的信息不会凭空出现。

### 4.3 限时工作范围（租约）

Agent 写入前会自动从 `apply_patch` 或常见 Shell 写入命令中提取目标路径，并领取尽可能小的限时写范围。长任务会自动续期，会话结束后自动登记实际变更并释放。

冲突规则是：

- 只读与只读可以重叠；
- 普通源码写范围重叠会产生警告，需要缩小范围或给出明确协调理由；
- Unity 场景、Prefab、资源、`.meta`、项目配置和 Luban 等高风险范围采用严格独占，重叠时直接拒绝写入；
- 目录与其子文件视为重叠；Windows 路径按不区分大小写处理；
- 每个 Agent 会话只使用自己领取的租约；即使两个 Agent 属于同一成员，也会作为两个独立会话检查重叠，不能借用、续期或关闭对方的租约；
- 从旧版本数据库迁移而来的未绑定租约继续作为成员级占用保留到关闭或过期，新 Agent 会话不会自动继承；
- 租约到期只释放当前占用，不删除历史记录。

如果 Shell 命令可能写文件但系统无法确定路径，Hook 会暂停该命令，要求 Agent 改用明确路径或先领取范围。这是保护行为，不是程序故障。

### 4.4 写入后的真实变化检查

命令执行后，Hook 会重新检查 Git 工作区。若实际变化超出已领取范围，系统会登记一条风险并停止 Agent 继续新增修改，直到差异和范围问题被处理。

会话开始前已经处于 dirty 状态的文件也不会被简单忽略。本地组件会保存这些文件的内容与 Git 状态指纹；同一路径后来又发生变化时，仍会识别为当前会话产生的实际变更。

这可以覆盖“工具声称只改 A，实际上生成器或格式化器又改了 B”的情况。检测到越界后，当前 Codex 会话会进入隔离状态：读操作仍可继续，但后续受支持的写操作会持续被拒绝，而不是只提示一次。系统不会自动撤销已经发生的写入，避免错误覆盖成员现有工作；应先查看 Git 差异，再决定保留、协调或人工恢复，随后结束当前会话并从干净、已协调的状态重新开始。

### 4.5 本地扫描与依赖影响

后台扫描器当前会结合以下证据构建项目结构：

- Git 已跟踪文件、未提交变化和最近共改历史；
- C# 命名空间、类型引用和 using 关系；
- Unity GUID / `.meta` 资源引用；
- Unity `.asmdef` 程序集引用；
- `AGENTS.md`、`ProjectRules` 等规则文件的路径和哈希；
- 根据路径推导的系统划分与受影响系统。

扫描结果会帮助 Agent 发现“文件不重叠但系统可能耦合”的风险。例如 A 改动底层武器接口时，B 的 UI 或动画系统可能被列为受影响系统。扫描是风险提示，不是完整的编译器、Unity 运行时或业务语义证明。

### 4.6 决定、验证、风险与交接

房间可以保存有路径范围和证据的决定、验证结果、剩余风险及交接记录。这些信息会进入后续成员的快照，并保留作者和时间，避免关键约定只存在于某个人的聊天中。

Agent 可以自动登记工作范围和实际变更；必要时成员也可以在工作台手工补充记录。项目源码、测试输出和人工批准的规则仍高于 Agent 自己的观察。

通过 MCP 手工管理工作时，`session_open` 会返回唯一会话 ID；后续领取、续期、写入检查、登记事件和 `session_close` 都必须回传这个 ID。验证或交接只能关联当前会话自己的租约。关闭会话时，系统还会自动取消该会话遗漏的剩余租约，避免其他人被过期前的占用长期挡住。这样同一成员同时运行多个 Agent 时，日常协调不会误用或误关另一个会话。

### 4.7 共享分支门禁

仓库提供可选的 `gate:publish` 检查。团队在 GitHub Actions 中配置房间地址、成员凭证和共享分支后，门禁会检查：

- 未解决的 critical / blocking 风险；
- 阻塞级范围冲突；
- 同一验证范围中最新的失败结果。

功能分支不属于配置的共享分支时会跳过。共享分支在已启用门禁但无法验证房间状态时采用拒绝策略。仓库附带的 CI 和 Release workflow 在没有配置 Agent Hub 地址或凭证时会明确跳过门禁，以便首次发布不会自锁。

门禁检查的是房间中已经登记的状态，不能代替测试、代码审查和分支保护。

## 5. A 的修改影响 B 时会发生什么

一个典型流程如下：

1. A 的 Agent 启动时读取房间快照，并在修改前领取相关文件或系统范围。
2. B 的本地扫描把项目依赖和受影响系统同步到同一房间。
3. B 的 Agent 开始耦合功能时，会看到 A 的活跃范围、相关决定和风险。
4. 如果 B 要写入 A 占用的严格资源，写入前直接暂停；普通源码重叠则要求协调。
5. 如果工具实际修改了未领取文件，写入后检查会登记风险并停止继续修改。
6. 测试失败或留下 critical 风险时，可选共享分支门禁会阻止 CI/Release 通过。

这套流程能够把许多“事后才发现被改坏”的问题提前到“写入前或发布前”。但是如果 A 与 B 只在业务语义、网络协议、运行时顺序或未被扫描到的数据上耦合，系统可能无法仅靠静态信息发现。因此正确目标是“在保留别人功能的前提下提高完成新功能的成功率”，而不是承诺零回归。

## 6. 相比只有 AGENTS.md 的优势

| 能力 | 单一 `AGENTS.md` | Agent Hub |
| --- | --- | --- |
| 稳定编码规则 | 适合 | 仍然保留并读取 |
| 谁正在改什么 | 无法实时表达 | 活跃租约和成员状态 |
| 中途加入后的现场 | 依赖人工转述 | 读取当前房间快照 |
| 写入前冲突检查 | 只能要求 Agent 自觉 | Hook 自动检查并可拒绝 |
| 实际变更超范围 | 无法感知 | 写入后对照 Git 变化 |
| 决定、验证、风险 | 容易散落在聊天中 | 结构化、带作者和时间保存 |
| 项目依赖影响 | 通常靠人工维护 | 本地持续扫描并同步元数据 |
| 共享分支发布检查 | 文档本身不能执行 | 可选 CI 门禁 |
| 审计 | 只看到规则文件历史 | 追加式协作活动记录 |

两者不是替代关系。`AGENTS.md` 继续保存稳定、人工批准的项目规范；Agent Hub 保存不断变化的协作现场并执行检查。最可靠的方式是让项目规则明确要求使用 Agent Hub，同时由安装版自动完成日常接入。

## 7. 能力边界

Agent Hub 能够：

- 让多个 Agent 读取同一份实时、结构化项目状态；
- 自动登记和保护计划修改范围；
- 对高风险资源执行严格互斥；
- 提示代码、Unity 资源、程序集和历史共改关系；
- 让中途加入者接上已登记的工作；
- 在异常写入、失败验证和未解决风险出现时暂停或阻止发布流程。

Agent Hub 不能：

- 合并或读取不同模型的内部记忆、隐藏思考和私人对话；
- 证明某次修改在所有业务语义上绝对安全；
- 自动理解所有动态调用、反射、网络协议、运行时时序和策划数据含义；
- 代替编译、自动化测试、Unity Play Mode、真机验证、代码审查和人工验收；
- 代替 Git、分支策略、合并工具或项目权威数据源；
- 在房主离线、Hook 被禁用或客户端未加入房间时继续提供完整保护；
- 自动解决需要产品或架构负责人作决定的真实冲突。

当前 A 版面向可信团队并以防止误操作为目标，不把同一成员凭证下的多个本地客户端视为互相敌对的安全主体。会话 ID 用于准确归属，不是第二个秘密凭证；不要把成员凭证复制给其他人或不受信设备。需要抵抗同一成员凭证被恶意复用时，应在后续服务器版增加会话级能力凭证或与 MCP 连接绑定的身份校验。

当系统检测到冲突时，正确行为是暂停并把影响范围、负责人和证据展示给团队，而不是猜测哪一方应该被覆盖。

## 8. 对 Git 操作的影响

安装 Agent Hub 不会安装本地 Git commit Hook 或 pre-push Hook，不会改变提交内容，也不会自动合并、回滚或推送代码。成员仍按原有方式执行 `git add`、`git commit`、`git pull`、`git push` 和 Pull Request。

Codex Hook 保护的是 Agent 通过受支持写入工具修改文件的过程。可选共享分支门禁运行在 CI/Release 中：本地 push 可以照常发出，但远端检查可能因为房间中的阻塞项失败。处理完冲突、风险或失败验证后，重新运行检查即可。

没有接入 Hook 的编辑器、其他 Agent、手工文件操作和被人为绕过的客户端不受本地自动保护，因此团队仍应保留分支保护与代码审查。

## 9. 安装版与便携版

### Setup 安装版（推荐）

- 可以创建和加入房间；
- 可以保存本地仓库连接并执行后台扫描；
- 可以一键写入 Codex MCP 和四类协作 Hook；
- 安装后路径稳定，重启 Codex 后可以持续使用自动接入。

### Portable 便携版

- 可以临时创建或加入房间并查看工作台；
- 可以作为演示、诊断或临时访问使用；
- 不允许安装 Codex 自动接入，因为便携程序每次运行的解压路径可能变化；
- 若要让 Agent 在日常开发中自动领取范围和执行 Hook，请改用 Setup 安装版。

## 10. 局域网、HTTPS 与凭证安全

当前首轮验证采用房主电脑局域网模式：

- 房主服务监听本机可用端口，并在界面提供局域网邀请地址；
- HTTP 只应在可信专用网络中使用；
- 房间码只发给参与该项目的成员，且不能替代 Git 仓库权限；
- 成员凭证不应提交到 Git，也不应通过公开聊天或不可信网络发送；
- 路由器重新分配房主 IP 后，房主可能需要重新发送新的邀请地址。

不要通过端口映射把桌面版 HTTP 服务直接暴露到互联网。跨地域或生产使用需要持续在线的内网/服务器部署、HTTPS、访问控制、数据库备份和监控；这些不是当前桌面版的一键功能。

## 11. Windows SmartScreen 提示

当前 GitHub Release 安装包未使用商业代码签名证书，因此 Windows SmartScreen 可能拦截首次运行。这不代表程序一定有问题，但用户不应盲目绕过提示。

正确做法是：

1. 只从团队确认的 GitHub 仓库 Release 下载；
2. 使用 Release 附带的 `SHA256SUMS.txt` 核对文件哈希；
3. 确认发布者和版本无误后，再通过“更多信息”继续；
4. 来源不明或哈希不一致时立即停止运行。

未来使用可信代码签名证书后，可以改善安装身份验证和 SmartScreen 体验。

## 12. 推荐的事实优先级

当房间信息发生矛盾时，按以下顺序判断：

1. 当前 Git 状态、项目源码和项目权威数据；
2. 人工批准的 `AGENTS.md`、`ProjectRules` 和架构决定；
3. 当前活跃工作范围与已确认冲突处理；
4. 与具体提交绑定的验证结果；
5. Agent 或成员登记的观察和风险；
6. 历史聊天内容。

Agent Hub 是协作控制层，不是新的源码真相来源。任何“未发现范围重叠”都只能理解为“已登记信息中未发现”，不能表述为“修改绝对安全”。

## 13. 最简团队使用规则

1. 所有成员安装 Setup 版，选择自己的本地 Git 项目并加入同一房间。
2. 每位成员在“连接设置”中点击一次“安装连接”，重启 Codex，并在首次提示时核对后信任 Hook。
3. 房主开发期间保持 Agent Hub 运行；成员正常使用 Codex，无需手工领取和释放范围。
4. Agent Hub 暂停写入时，不要绕过；先看冲突成员、路径、决定、验证和风险。
5. 完成改动后仍执行项目规定的测试和真实运行时验证。
6. 共享分支继续使用 Pull Request、代码审查和分支保护；需要更强约束时再配置 CI 门禁。
7. 不把房间码、成员凭证、密钥、私人聊天或源码全文写进协作记录。

做到以上设置后，大部分日常协调由产品自动完成；只有真正存在冲突或证据不足时，才把决定交还给协作成员。
